---
layout: base.njk
---

<!-- Hero Section -->
<div class="hero p-8 md:p-16 bg-gradient-to-b from-primary/50 to-bg">
  <div class="hero-content text-center text-balance">
    <div class="w-full lg:max-w-md">
      <img src="/assets/logo.png" alt="Inflection Engineering" class="w-80 mx-auto object-contain m-4"/>
      <p class="py-0 my-2 italic">Helping CTOs adapt faster, when it matters most.</p>
      <p class="mt-12">We help scale-ups adapt faster when success starts to slow them down. You're already in the race of
        your life — we're the boost that gets you back on track.</p>
      <div class="mt-12">
        <a
          class="btn btn-soft btn-accent plausible-event-name=ShowContactModal"
          href="mailto:hello@inflection.engineering?body=Hi%20folks,%0A%0A"
          onclick="get_in_touch.showModal(); event.preventDefault();">Get in touch</a>
        <a class="btn btn-ghost" href="#why-us">Learn more ↓</a>
      </div>
    </div>
  </div>
</div>
<!-- Carousel -->
<section class="py-16 w-full">
  <div class="flex w-16 md:w-full mx-auto justify-center gap-4 py-4 md:hidden">
    {% for item in collections.homepageCarousel %}
      <a href="#{{ item.fileSlug | removeNumberPrefix }}" class="btn btn-xs">{{ loop.index }}</a>
    {% endfor %}
  </div>
  <div class="relative">
    <div class="carousel carousel-center w-full gap-[20%]">
      {% for item in collections.homepageCarousel %}
        <div id="{{ item.fileSlug | removeNumberPrefix }}" class="carousel-item relative w-3/4 card rounded-lg bg-primary/25 {% if loop.first %}ms-[20%]{% endif %}{% if loop.last %} me-[20%]{% endif %}">
          <div class="card-body text-base flex-none">
            <h3 class="card-title mt-0 mb-4">{{ item.data.title }}</h3>
            {{ item.content | safe }}
            <a class="btn btn-accent w-32 max-w-full self-end" href="{{ item.data.cta.link }}">{{ item.data.cta.title }} &raquo;</a>
          </div>
        </div>
      {% endfor %}
    </div>
    <button
      class="btn btn-ghost btn-accent absolute left-0 top-0 h-full rounded-none active:btn-primary hover:btn-soft
        hover:btn-primary md:btn-soft md:left-12 md:top-1/2 md:!-translate-y-1/2 md:h-auto md:!btn-circle"
      id="carousel-prev">
      <span class="h-full overflow-y-clip flex items-start pt-24 pb-20 md:block md:h-auto md:overflow-visible md:p-0">
        <span class="sticky top-1/2 -translate-y-1/2 md:static md:translate-y-0">❮</span>
      </span>
    </button>
    <button
      class="btn btn-ghost btn-accent absolute right-0 top-0 h-full rounded-none active:btn-primary hover:btn-soft
        hover:btn-primary md:btn-soft md:right-12 md:top-1/2 md:!-translate-y-1/2 md:h-auto md:!btn-circle"
      id="carousel-next">
      <span class="h-full overflow-y-clip flex items-start pt-24 pb-20 md:block md:h-auto md:overflow-visible md:p-0">
        <span class="sticky top-1/2 -translate-y-1/2 md:static md:translate-y-0">❯</span>
      </span>
    </button>
  </div>
  <div class="hidden md:flex w-full mx-auto justify-center gap-4 py-4">
    {% for item in collections.homepageCarousel %}
      <a href="#{{ item.fileSlug | removeNumberPrefix }}" class="btn btn-xs">{{ item.data.title }}</a>
    {% endfor %}
  </div>
</section>
{% include "get_in_touch.njk" %}
<!-- Footer -->
<footer class="footer footer-center p-10 text-base-content">
  <p class="text-base-content/45">Inflection Engineering &copy; 2024-{{ "now" | date("YYYY") }}</p>
</footer>